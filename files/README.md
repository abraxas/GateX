# files/

`7350FH.zip` is **not** in this repository. Copy the CTF zip here:

```
files/7350FH.zip
```

GateX looks at that path by default (`/target files/7350FH.zip`). Zips and native binaries are gitignored.

| Path | What |
|---|---|
| `evidence/` | Live cage screenshots + transcripts after `/bypass` |
| `hunter-cli/` | `--version` / `--help` / `list` / `score --ml` dumps from the patched inner ELF |
